# Subflow Composer Extension for n8n

Create reusable subflows from node selections in the n8n editor.

## Structure

- `src/frontend/` — Vue components and frontend extension (via `@n8n/extension-sdk`)
- `src/backend/` — Backend extension (stub, SDK pending)
- `n8n.manifest.json` — Extension manifest
